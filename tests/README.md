# Tests

Run from the repository root:

```bash
pytest -q
```

The test suite currently checks:

- API health endpoint
- valid prediction response
- invalid input rejection
- fallback predictor probability behavior
